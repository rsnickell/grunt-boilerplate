module.exports = {
    execute_shell: {                    // Target
        options: {                      // Options
            stdout: true
        },
        command: 'echo "  shell command executed"'
    }
};
